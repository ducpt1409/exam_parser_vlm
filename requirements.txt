# ===== exam_parser_vlm — Python deps (conda env: exam_parser_vlm) =====
# LƯU Ý: KHÔNG cài torch/vllm bằng file này. Xem SETUP.md để cài đúng CUDA 12.8 (Blackwell sm_120).
# File này chỉ chứa deps của pipeline parse (chạy được kể cả khi vLLM chạy ở tiến trình riêng).

# --- Document / image ---
pymupdf>=1.24.0           # render PDF -> ảnh
pillow>=10.2.0            # crop, stitch, encode
opencv-python>=4.9.0      # deskew + CV box-snap
numpy>=1.26.0

# --- VLM client ---
openai>=1.40.0            # gọi vLLM qua OpenAI-compatible API (/v1)
httpx>=0.27.0             # gọi Ollama /api/chat (backend phụ)

# --- Config / schema / CLI / log ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
loguru>=0.7.2
click>=8.1.7

# --- (giai đoạn sau, chưa bắt buộc cho Phase 3) ---
# fastapi  uvicorn[standard]  python-multipart   # API service
# minio                                          # object storage
# celery  redis                                  # job queue
